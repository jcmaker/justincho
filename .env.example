# Public base URL of the deployed site.
# Used as `metadataBase` so Open Graph / Twitter image paths resolve to absolute URLs.
# Set this to your production domain when deploying (e.g. https://justincho.dev).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
